bindAddr = "127.0.0.1"
bindPort = 17000

auth.method = "token"
auth.token = "smoke-test-token"

webServer.addr = "127.0.0.1"
webServer.port = 17500
webServer.user = "admin"
webServer.password = "admin"

log.level = "info"
log.to = "./frps_smoke.log"
